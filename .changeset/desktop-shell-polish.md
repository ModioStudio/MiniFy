---
"MiniFy": patch
---

Load every track of a playlist instead of the first page, with progress while it streams in; tint the Windows title bar with the active theme; keep the mini player's visualizer in sync with the desktop settings; and show why a search or playlist load failed instead of an empty list.
