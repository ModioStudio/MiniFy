---
"MiniFy": patch
---

Fix Linux AppImage packaging of the bundled YouTube audio helpers.

- Let linuxdeploy find yt-dlp's bundled libraries during the Linux release build.
- Enable verbose Linux build output to expose packaging errors.
