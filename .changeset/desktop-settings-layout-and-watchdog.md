---
"MiniFy": patch
---

Quit MiniFy when its last window closes instead of leaving a hidden process behind, lay desktop settings out as one full-width scrolling page, hide scrollbar arrow buttons, keep skipping through songs from reaching the embedded music video player, and record freezes and long blocking tasks in `diagnostics.log`.
